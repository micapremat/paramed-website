---
import { getCollection } from "astro:content";
import { Picture } from "astro:assets";
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";

// Filter team entries with 'draft: false' & date before current date
const publishedTeamMembers = await getCollection("team", ({ data }) => {
  return !data.draft && data.publishDate < new Date();
});
---

<Layout title="About">
  <Container>
    <Sectionhead>
      <Fragment slot="title">Instituto ParaMED</Fragment>
      <Fragment slot="desc">Anios de Experiencia formando profesionales</Fragment>
    </Sectionhead>

    <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-gray-800">
       Sobre nosotros
      </h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Somos un equipo interdisciplinario de profesionales del área de la salud, con más de 20 años de experiencia en la formación de Técnicos en Salud.

        Desde este rol docente, apostamos a la formación de profesionales técnicos críticos, creativos comprometidos con la realidad y capaces de transformarla, asumiendo responsabilidades de asistencia, atención, gerenciamiento, gestión y planificación de acciones en los diferentes ámbitos de la salud pública.
        
        Nuestra Propuesta es abordar las distintas asignaturas del diseño curricular, desde una perspectiva teórico-práctica. Desde el 1ª año de la carrera el alumno tiene pleno ejercicio e inserción en las actividades propias y diarias del ámbito hospitalario especifico para el que se forma.
      </p>
    </div>
    <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
      {
        publishedTeamMembers.map((teamMemberEntry) => (
          <div class="group">
            <div class="w-full aspect-square">
              <Picture
                src={teamMemberEntry.data.avatar.src}
                alt={teamMemberEntry.data.avatar.alt}
                sizes="(max-width: 800px) 100vw, 400px"
                width={400}
                height={400}
                class="w-full rounded-md rounded transition group-hover:-translate-y-1 group-hover:shadow-xl bg-white object-cover object-center aspect-square"
              />
            </div>

            <div class="mt-4 text-center">
              <h2 class="text-lg text-gray-800">{teamMemberEntry.data.name}</h2>
              <h3 class="text-sm text-slate-500">
                {teamMemberEntry.data.title}
              </h3>
            </div>
          </div>
        ))
      }
    </div>
  </Container>
</Layout>
